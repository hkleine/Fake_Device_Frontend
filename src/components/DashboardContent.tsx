import React from 'react';

const DashboardContent = ({ children }: any) => (
  <main className="w-full h-full bg-gray-200 bg-opacity-75 pt-12 px-16">{children}</main>
);

export default DashboardContent;
